AnnotationInjector is a small Java library that allows to inject annotations in classes at runtime.
I forked original project and modified it to work in Java 8.
It is mostly a technical experiment and NOT intended for production use.

